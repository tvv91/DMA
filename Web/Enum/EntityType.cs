namespace Web.Enum
{
    /// <summary>
    /// Defines where we should find image
    /// </summary>
    public enum EntityType
    {
        AlbumCover,
        VinylState,
        DigitalFormat,
        Bitness,
        Sampling,
        SourceFormat,
        Player,
        Cartridge,
        Amp,
        Adc,
        Wire,
        AlbumDetailCover
    }
}
